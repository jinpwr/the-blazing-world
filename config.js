/* The only file you need to edit after forking.
   owner/repo/branch are used by the editor at /admin.html to save posts. */
window.SITE = {
  title:  "Build Log",
  tagline: "Notes from projects in progress — what shipped, what broke, what I'd do differently.",
  owner:  "YOUR-GITHUB-USERNAME",
  repo:   "YOUR-REPO-NAME",
  branch: "main"
};
